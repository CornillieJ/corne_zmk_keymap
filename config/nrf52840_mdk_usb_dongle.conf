# Unlike nice_nano_v2, this board has no ZMK-native board definition, so
# UF2 output isn't enabled by default and the build silently falls back to
# a raw .bin file instead (which a UF2 bootloader can't flash). This
# applies to every build targeting this board (corne_dongle, settings_reset).
CONFIG_BUILD_OUTPUT_UF2=y
