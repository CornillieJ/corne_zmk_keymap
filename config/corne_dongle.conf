# The dongle has no OLED wired up; disable the display feature that
# corne.conf enables for the physical halves.
CONFIG_ZMK_DISPLAY=n

# Temporary: log BLE connect/disconnect/bond activity over the dongle's
# USB serial port to diagnose the peripheral connection issue.
CONFIG_ZMK_USB_LOGGING=y
